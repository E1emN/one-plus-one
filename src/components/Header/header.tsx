import React from 'react';
import './header.scss';
import { api } from '../../utils/api';

export const Header: React.FC = () => {
 
    return(
        <header>
            Hello World!sdss
        </header>
    )
};